# R069's exact 12k gate; only the iteration horizon changes.

CONFIG_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=configs/r069_guide_support_gauge_0_30k.env
source "${CONFIG_DIR}/r069_guide_support_gauge_0_30k.env"

ITERATIONS=12000

unset CONFIG_DIR
