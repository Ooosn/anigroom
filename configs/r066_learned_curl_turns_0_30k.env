# R066 is a code-only primary-guide curl-turns experiment from R065.
# This file intentionally sources R065 and defines no environment override.

CONFIG_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=configs/r065_local_crossing_residual_0_30k.env
source "${CONFIG_DIR}/r065_local_crossing_residual_0_30k.env"
unset CONFIG_DIR
