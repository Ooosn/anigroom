# R065 is a strict code-only correction of R064 on top of accepted R062.
# The crossing active set, score, weight, and refresh schedule are unchanged.
# Only gradient ownership changes: a local crossing updates the active dense
# zero-centered shape residual instead of the shared primary guide field.

CONFIG_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=configs/r062_mesh_no_penetration_0_30k.env
source "${CONFIG_DIR}/r062_mesh_no_penetration_0_30k.env"
unset CONFIG_DIR

STRAND_CROSSING_SUPPORT=1
STRAND_CROSSING_WEIGHT=0.001
STRAND_CROSSING_REFRESH_INTERVAL=2000
STRAND_CROSSING_QUERY_BATCH=50000
STRAND_CROSSING_EXACT_PAIR_BATCH=250000
