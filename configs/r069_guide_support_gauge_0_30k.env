# R069 is the exact R068 child with only guide-support-gauge weight enabled.

CONFIG_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=configs/r068_no_crossing_zero_curl_0_30k.env
source "${CONFIG_DIR}/r068_no_crossing_zero_curl_0_30k.env"

GUIDE_SUPPORT_GAUGE_WEIGHT=0.001

unset CONFIG_DIR
