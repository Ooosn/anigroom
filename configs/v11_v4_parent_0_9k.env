# Controlled v11-v4 experiment: exact 0-9k prefix of the accepted v11 parent.
#
# The only intended method delta from the exact v11 reproduction is the
# accepted v4 clean-flow target plus its normal-compatible, parallel-transport
# runtime interpolation. All schedules through iteration 9000 remain inherited
# from the formal 30k parent route.
source "$(dirname "${BASH_SOURCE[0]}")/white_tiger_stage1_cleanflow_multiview_30k_rgbflow.env"

ITERATIONS=9000
