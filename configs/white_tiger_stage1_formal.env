# White Tiger Stage 1 formal foundation config.
#
# This intentionally points to the active 38+ Stage1 execution candidate:
# 100k render roots, low guide roots, render densification first, delayed guide
# densification, and no old screen/dark/luma overlong heuristic route.
# Current validation target: full-resolution view09 single-view correctness.

source "$(dirname "${BASH_SOURCE[0]}")/white_tiger_stage1_multiroot_stageab_100k_lowguide_view09.env"
