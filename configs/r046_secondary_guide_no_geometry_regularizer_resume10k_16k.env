# R046 causal upper-bound diagnostic.
# Resume the exact R044 10k state and remove only explicit secondary-geometry
# regularization. Primary-guide, appearance, RGB, lifecycle, and interpolation
# behavior remain unchanged.

CONFIG_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=configs/r045_secondary_guide_k4_resume10k_16k.env
source "${CONFIG_DIR}/r045_secondary_guide_k4_resume10k_16k.env"

GEOMETRY_RESIDUAL_SMOOTH_SCALE=0.0
EFFECTIVE_SMOOTH_WEIGHT=0.0
CLEAN_FLOW_3D_SMOOTH_WEIGHT=0.0
