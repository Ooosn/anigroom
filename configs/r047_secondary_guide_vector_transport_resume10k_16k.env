# R047 causal fix validation.
# Inherit the R046 diagnostic exactly; the only experimental variable is the
# differentiable vector-field transport implemented in code.

CONFIG_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=configs/r046_secondary_guide_no_geometry_regularizer_resume10k_16k.env
source "${CONFIG_DIR}/r046_secondary_guide_no_geometry_regularizer_resume10k_16k.env"
unset CONFIG_DIR
