# R080 all-low-frequency-guide unlock continuation. Preserve the complete
# R079 3k->4k contract and release the normal low-frequency guide group
# together at iteration 3001.

CONFIG_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=configs/r079_length_unlock_at3k_resume.env
source "${CONFIG_DIR}/r079_length_unlock_at3k_resume.env"

GUIDE_FREEZE_UNTIL=3000

unset CONFIG_DIR
