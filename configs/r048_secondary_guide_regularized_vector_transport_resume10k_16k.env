# R048 formal candidate validation.
# Restore the completed R045 local K4 regularization configuration exactly;
# the only variable relative to R045 is R047's differentiable vector transport.

CONFIG_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=configs/r045_secondary_guide_k4_resume10k_16k.env
source "${CONFIG_DIR}/r045_secondary_guide_k4_resume10k_16k.env"
unset CONFIG_DIR

